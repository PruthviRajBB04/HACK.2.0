import { Link } from '@tanstack/react-router'
import { ArrowLeft, CheckCircle2 } from 'lucide-react'
import type { ReactNode } from 'react'
import { Brand } from '@/components/Brand'

export function AuthLayout({ eyebrow, title, description, children }: { eyebrow: string; title: string; description: string; children: ReactNode }) {
  return <main className="min-h-screen bg-[#f5f3ec]"><div className="grid min-h-screen lg:grid-cols-[.82fr_1.18fr]"><aside className="hero-mesh hidden flex-col justify-between overflow-hidden p-10 text-white lg:flex"><Brand inverse/><div className="max-w-lg animate-rise"><p className="text-xs font-bold uppercase tracking-[.24em] text-amber-300">Governance, made visible</p><h2 className="mt-5 font-display text-5xl font-semibold leading-[1.08]">One operational picture. Every compliance signal.</h2><div className="mt-8 space-y-3 text-sm text-emerald-50/80">{['Provider-ready authentication architecture','Role-aware application navigation','Centralized, replaceable demonstration data'].map(item=><div key={item} className="flex items-center gap-3"><CheckCircle2 className="size-4 text-amber-300"/>{item}</div>)}</div></div><p className="text-xs text-emerald-100/50">Prototype interface — not an official government platform</p></aside><section className="flex items-center justify-center p-4 sm:p-8"><div className="w-full max-w-2xl"><div className="mb-8 flex items-center justify-between lg:hidden"><Brand/><Link to="/" className="flex items-center gap-2 text-sm font-semibold text-slate-600"><ArrowLeft className="size-4"/>Home</Link></div><div className="rounded-3xl border border-slate-200 bg-white p-6 shadow-[0_30px_80px_rgba(33,48,42,.12)] sm:p-9"><p className="text-xs font-bold uppercase tracking-[.2em] text-amber-700">{eyebrow}</p><h1 className="mt-3 font-display text-3xl font-semibold text-emerald-950 sm:text-4xl">{title}</h1><p className="mt-3 max-w-xl text-sm leading-6 text-slate-500">{description}</p><div className="mt-8">{children}</div></div></div></section></div></main>
}
